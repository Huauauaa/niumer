# Default work-hour integration (merged with config.<NIUMER_ENV>.yaml).
# Override any value per environment in config.dev.yaml / config.prod.yaml.
# Runtime env vars WORK_HOUR_* still override these (see README).

workhour:
  login_url: "http://127.0.0.1:17890/login"
  wait_css: ".search-total__num"
  tenant_url: "http://127.0.0.1:17890/id"
  # POST /user-info (envelope: status, data.hrId, shiftNameZh, ok).
  user_info_url: "http://127.0.0.1:17890/user-info"
  workhour_url: "http://127.0.0.1:17890/work-hour"
  # GET with ?page=&page_size=; local mock: cmd/mockserver GET /pull-request
  pull_request_list_url: "http://127.0.0.1:17890/pull-request"
